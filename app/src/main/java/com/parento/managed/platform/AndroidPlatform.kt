package com.parento.managed.platform

/**
 * Boundary for Android-specific integrations.
 *
 * Future implementations should use documented Android APIs and request
 * runtime permissions only when an associated feature is actually enabled.
 */
interface AndroidPlatform
